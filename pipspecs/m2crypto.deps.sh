#!/bin/bash
# Next field not needed, as builddeps is added to deplist automatically
# at the moment.
#
# TODO: Clear custom additions to builddeps automatically, so -devel packages
#       don't appear in real deps.
#echo openssl >> /root/deplist.txt
